{
  "name": "facebook-profile-posts-all-photos-scraper",
  "version": "1.0.0",
  "description": "Apify Actor for public Facebook personal-profile posts with agent-ready JSON, all recoverable photos, monitoring boundaries, and cursor backfills.",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "backfill:profile": "node scripts/backfill-profile.mjs",
    "check": "node --check src/main.js && node --check src/charging.js && node --check src/facebook.js && node --check src/postParser.js && node --check src/mediaExpansion.js && node --check src/profileRetry.js && node --check scripts/backfill-profile.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "apify": "^3.4.0",
    "got-scraping": "^4.1.2",
    "html-entities": "^2.6.0",
    "tough-cookie": "^5.1.2"
  },
  "devDependencies": {}
}
